
export class LearnByTypingSettings {
    languagePackName: string
    packNumber: number

    constructor(languagePackName: string, packNumber: number) {
        this.languagePackName = languagePackName
        this.packNumber = packNumber
    }
}